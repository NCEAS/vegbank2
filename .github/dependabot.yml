version: 2
updates:
  - package-ecosystem: "uv"
    directory: "/"
    target-branch: "develop"
    schedule:
      interval: "daily" # Checks every day for any update (including security ones)
    groups:
      # Grouping ensures we don't get 50 individual PRs
      all-dependencies:
        patterns:
          - "*"
